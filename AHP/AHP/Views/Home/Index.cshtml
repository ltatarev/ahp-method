
    <div class="row text-light pt-5 mt-2 text-left">
        <div class="col text-center text-md-left">
            <h1 class="ml3" id="MainTitle">AHP METHOD</h1>
            <p class="lead font-italic" style="letter-spacing:2px; font-size:18px;"></p>
        </div>
    </div>
        <div class="row text-center text-md-left">
            <div class="col">
                <input type="button" value="Learn more" class="btn btnLetter btn-light btn-learn" />
            </div>
        </div>
        <div class="row pt-5 text-center mt-5 mr-4 float-right" style="border-radius:25px;">
            <div class="col">
                <!-- should redirect to "Login" -->
                <input type="button" value="New Project" onclick="location.href='@Url.Action("Login","Home")'" class="btn btnLetter btn-success" />

                <!-- should redirect to "AllProjects" -->
                <input type="button" value="Open existing project" onclick="location.href=' @Url.Action("AllProjects","Home")'" class="btn btnLetter btn-outline-light  ml-2" />
            </div>
        </div>
    
@section scripts {
    <script>
        $(document).ready(function () {
            $(".btn-learn").on("click", function () {
                
                $.confirm({
                    title: 'How to use',
                    content:                        
                        `&#9679; <strong> Create a new <em>project</em> you need a decision on, or pick an existing one</strong> (20%)
                         &#9679; <strong> Set <em>criteria</em> on which you'll rank your alternatives </strong> (40%)
                         &#9679; <strong> <em>Rank</em> your <em>criteria</em> based on importance </strong> (60%)
                         &#9679; <strong> Set your <em>alternatives</em> as options between which you wanna choose the best one </strong> (80%)
                         &#9679; <strong> <em>Rank</em> your alternatives based on each criterion </strong> (80%) 
                         &#9679; <strong> Get your <em>final decision</em> on which alternative is the best choice </strong> (100%)`.split('\n').join('<br>'),
                       
                                                               
                   
                    type: 'blue',
                    typeAnimated: true,
                    buttons: {
                        tryAgain: {
                            text: 'Ok',
                            btnClass: 'btn-blue',
                            action: function () {
                            }
                        }
                        
                    }
                });
            })
        });



        // Wrap every letter in a span
        var textWrapper = document.querySelector('.ml3');
        textWrapper.innerHTML = textWrapper.textContent.replace(/([^\x00-\x80]|\w)/g, "<span class='letter'>$&</span>");

    anime.timeline({ loop: false })
            .add({
                targets: '.ml3 .letter',
                opacity: [0, 1],
                easing: "easeInOutQuad",
                duration: 1200,
            })
                //.add({
            //    targets: '.hidden',
            //    opacity: '100',
            //    easing: 'easeInOutQuad',
            //    duration: 1200
            //})
    

  //  .add({
  //  targets: '.ml3',
  //  opacity: 0,
  //  duration: 1000,
  //  easing: "easeOutExpo",
  //  delay: 1000
  //});
    </script>
}
